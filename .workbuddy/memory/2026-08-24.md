- 对项目进行只读清理扫描并在用户确认后执行安全清理：已备份 13 个清理候选到 `C:\Users\ybd06\temp\newapi-tray-monitor-cleanup-20260824-164029`，校验通过。
- 已从项目目录移走 `build/`、`debug.log` 和两个 `vite.config.js.timestamp-*.mjs` 临时文件；剩余旧 `release*` 目录因批量回收站接口返回错误码 2 已停止处理，未继续重试。
- `node_modules/`、`dist/`、`src-tauri/target/`、`overview.md` 未修改；后续若继续，应逐个处理剩余 6 个旧发布目录并逐项核验。
- 新增余额提示阈值功能：设置页保存 `balanceAlertThreshold`（默认 5），主界面余额严格低于阈值时显示红色；使用 `npm run build:tauri:dev` 验证通过，普通 `npm run build` 因现有 dist/assets 安全清理失败未完成。
- 已完成 Windows x64 NSIS 安装包构建：`src-tauri/target/release/bundle/nsis/NewAPI监控_1.0.0_x64-setup.exe`，约 3.5 MB，SHA-256 为 `3a005da3591ff6ad8cbe4d8422c6542aceab158da0ca06d00dece6c68d8e5996`。
- 修复设置窗口高度不足和余额阈值修改后主界面不刷新的问题：设置窗口高度调整为 680/最小 560，保存配置通过 `config-updated` 事件通知 dashboard 重新读取阈值。
