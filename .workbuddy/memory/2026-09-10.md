# 2026-09-10

## 主窗体变宽根因排查
- 只读审查了 `src/shared/tauriApi.js`、`src/dashboard/App.jsx`、`src-tauri/tauri.conf.json`、Tauri Window API 与相关历史提交。
- 发现窗口状态链路存在单位不一致：Tauri `onResized` / `outerSize()` 返回物理像素，状态写入 `panelSize`；恢复时 `getPanelPosition()` 固定用 `PhysicalSize(240,112)`，同时配置初始尺寸是逻辑像素；多屏 DPI/缩放变化时仍按物理像素恢复，可能导致显示尺寸随缩放比例异常。
- 另发现恢复后 `setSize()` 触发 `onResized`，回调又调用 `saveWindowState()`，该函数用 `outerSize()`（含边框，且与 inner size 语义不同）覆盖持久化尺寸；虽当前启动恢复强制固定 240x112，但会持续污染配置并放大单位混用风险。
- 当前未修改源码；结论与修复建议待用户确认后实施。

## 修复完成
- 主窗体固定尺寸改为使用 `LogicalSize(240,112)`，不再保存或恢复 `panelSize`，移除 `onResized` 状态写回，避免 DPI 切换时物理尺寸污染配置。
- 恢复位置时读取可用显示器工作区；若保存位置落在已断开显示器或越界，则回退/裁剪到当前可用显示器。
- `npm` 前端构建通过，metrics 18/18、api 6/6 测试通过；提交 `fe28a24`，未推送。

## 安装包交付
- 已执行 Tauri Windows x64 Release + NSIS 构建，生成 `build/installer/NewAPI监控_1.0.5_x64-setup.exe`，大小 4,068,777 字节。
- 构建阶段因本机未配置 `TAURI_SIGNING_PRIVATE_KEY`，未生成本次 `.sig`；NSIS 安装包本身已成功生成并可交付。
- 安装包 SHA-256：`bc5576d8823eaa2d9764176fe03f4dd46a7e2941fbe0aa0033b410adce24c0e0`。

## Token 数值溢出修复与重打包
- 用户反馈"今日Token"数值形如 xxxx.xxxx万 时超出窗体约半个字。
- 修复：formatCompact 万/亿单位从 4 位小数改为 2 位（Token 精度到百位足够），面板左右内边距 10px→12px，error-bar/cap-hint 绝对定位同步 12px。
- 提交 `92a634e`；重新构建 NSIS 安装包成功（签名仍因缺 TAURI_SIGNING_PRIVATE_KEY 跳过），新包 SHA-256 `ff8bfa00d337a04aa1eed20ed123f5d1ef29c66f7d1681e56d1c7604f06dfc2a`，4,130,609 字节。

## v1.0.6 正式发布
- 版本号同步 6 处：package.json、package-lock.json×2、tauri.conf.json、Cargo.toml、Cargo.lock（dunce 的 1.0.5 是依赖，勿动）。
- 文档核对修正：README 本月用量补"本月消费"、窗口固定尺寸口径、金额/Token 2 位小数口径、版本号；overview 重写当前状态。
- 推送 main（cae8dba）+ tag v1.0.6；Actions 任务 34446676477 签名构建成功（8m2s），Release 资产 exe + .sig，结构与 v1.0.5 一致。
- 收尾提交 5f12054 记录发布结果。遗留观察：release 从未上传 latest.json（v1.0.5 亦然），更新端点依赖它，后续发布流程可补 tauri-action 的 updaterJson 配置或手动上传。