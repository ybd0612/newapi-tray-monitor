- 监控面板改为分组布局：余额单独突出显示；消费展示总消费/本月消费/今日消费；请求展示总请求数/本月请求数/今日请求数；Token展示本月Token/今日Token。
- 同步修正数据字段：总请求数取用户接口 request_count，本月请求数取月度聚合；新增月度消费金额聚合。
- 验证：metrics 单元测试 18/18 通过，Vite 前端构建通过；直接导入 Tauri API 的 Node 检查因缺少 window 运行时失败，属于测试环境限制。
- 方案随后调整为双态紧凑布局：第一行固定余额/总消费，第二行默认今日消费/请求量/Token，点击第二行切换本月，再次点击切回今日；实现提交 87d2cf8，QA 验证指标测试18/18、API mock测试6/6、构建通过。
- 已生成正式 Windows x64 NSIS 安装包：src-tauri/target/release/bundle/nsis/NewAPI监控_1.0.3_x64-setup.exe，约3.5MB。
- 后续修复第一行字号/对齐与窗口底部空白：统一两行三列网格和字号，主窗口 height=124、minHeight=112；兼容迁移旧版默认304x212到304x124，提交 4004ce2、051eb15。最终安装包已重新构建；指标18/18、API6/6通过。
- 透明拖拽区域根因是主窗口可纵向自由拉伸（resizable=true、无 maxHeight），且 dashboard-root 强制 height:100%。已改为 height:auto，并限制 maxHeight=124、保留横向调整，提交 1ecd394；已重新构建安装包。
- 用户进一步要求固定最小尺寸且禁止手动调整；最终固定窗口为 240x112、resizable=false、启动时清理历史尺寸状态，提交 b237dd3；安装包已重新构建。
- 用户反馈固定112px时上下边距不一致、底部截断；重新按内容盒模型校准为固定240x136，面板最小高度136、运行时尺寸同步，提交 eb0dcaf；metrics18/18、api6/6和Tauri安装包构建通过。
- 用户确认回到紧凑方案：固定240x112、不可调整；面板自然高度，错误/分页提示改为绝对定位不撑高布局，提交 4ab2396；metrics18/18、api6/6、Tauri安装包构建通过。
- 按零成本方案接入 Tauri updater/process、托盘确认安装、GitHub Actions 签名发布流程，提交 0a393a5；因尚未配置真实公钥且本地 npm 依赖安装遇到 supports-preserve-symlinks-flag@1.0.1 ETARGET，当前未生成自动更新签名安装包。
- 2026-09-07：补齐 `yaml@2.9.0` 开发依赖并增加 `tauri` 兼容 npm 脚本，修复 GitHub Actions 发布流程；任务 `34101130988` 成功完成，Release `v1.0.4` 已发布 NSIS 安装包与 `.sig` 签名文件。提交 `ec9507b`，标签 `v1.0.4` 已同步远程。
