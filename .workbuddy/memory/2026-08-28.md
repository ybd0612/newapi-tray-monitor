# 2026-08-28

- 调整 dashboard 原“今日消费”卡片：位置不变，标签改为“总消费 / 今日消费”，展示总消费 `usedAmount` 与今日消费 `todayAmount`，均保留两位小数。
- 修复指标推送链路：`collectMetrics` 将 `parseUser` 返回的 `usedAmount` 传给 dashboard。
- 验证：metrics 单元测试 18 项全部通过；Vite 前端构建通过；`git diff --check` 通过。
- 打包：Tauri Windows x64 NSIS 安装包构建成功，产物为 `src-tauri/target/release/bundle/nsis/NewAPI监控_1.0.2_x64-setup.exe`（约 3.7 MB），工作区保持干净。
- 余额展示精度调整为保留两位小数，仅修改 dashboard 的余额格式化，前端构建验证通过。
- 重新打包：生成包含余额两位小数修改的 Windows x64 NSIS 安装包，绝对路径为 `C:\Users\ybd06\Documents\project\newapi-tray-monitor\src-tauri\target\release\bundle\nsis\NewAPI监控_1.0.2_x64-setup.exe`，约 3.7 MB。
