{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the NewAPI monitor desktop windows.",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "autostart:default",
    "http:default"
  ]
}
